# 1. Clone your repo
git clone https://github.com/YourUsername/sddm-neonsky-theme.git
cd sddm-neonsky-theme

# 2. Run your custom install script
sudo ./install.sh

# 3. Apply the theme (or just reboot)
sudo systemctl restart sddm.service
